import { Box, Card, CardMedia, Fade } from '@mui/material'
import React from 'react'

const MLImageCard = () => {
  return (
   <Box sx={{width: '100%', 
      border: '1px solid red', 
      borderRadius: 10,
      height: '100%',
      position: 'relative',
      overflow: 'hidden',
      flex: 3,   
   }}>
   </Box>
  )
}

export default MLImageCard
