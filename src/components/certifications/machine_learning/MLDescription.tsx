import { Box } from '@mui/material'
import React from 'react'

const MLDescription = () => {
  return (
   <Box sx={{width: '100%', 
      border: '1px solid red', 
      borderRadius: 10,
      height: '100%',
      position: 'relative',
      overflow: 'hidden',
      flex: 3,
      }}>
      Hello
   </Box>
  )
}

export default MLDescription
